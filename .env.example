# Telegram Bot Token from @BotFather
TELEGRAM_BOT_TOKEN=your_telegram_bot_token_here

# Anthropic API key (for Claude + MCP)
# https://console.anthropic.com/
ANTHROPIC_API_KEY=

# Swiggy OAuth access_token for MCP (required for tool calls: search, cart, order)
# Set to the "access_token" from your OAuth response (token_type is Bearer).
# If MCP Inspector shows "Failed to discover OAuth metadata", use Guided OAuth with
# discovery URL: https://mcp.swiggy.com/.well-known/oauth-authorization-server
SWIGGY_AUTH_TOKEN=
